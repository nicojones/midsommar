export const STORAGE_KEY = {
  /**
   * Store the WEB password
   */
  pass: "_pass_",
  /**
   * Optimistically assume the user is logged in if they were logged in before
   */
  loggedIn: "_li_",
} as const;
